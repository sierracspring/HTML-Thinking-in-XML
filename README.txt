How do you think XML relates to HTML?
HTML is XML but XML is not HTML, meaning that HTML is a subset of XML.

What are some benefits of something like HTML using XML's syntax?
The benefits of this would be that XML is easier to understand from the viewpoint of a programmer.
XML also keeps content separate from presentatin.

What are some disadvantages?
XML isn't great for binary data, all data are strings, and it's syntax is redundant. 